import {Card} from './style'
export type CardProps = {
    description: string;
    director: string;
    producer: string;
}
export const index = ({description,director,producer}:CardProps) => {
  return (
    <Card>
       
    </Card>
  )
}
